# D1 第 4 步：大廳房間清單、加入、斷線即離開（中階，待審，未經跨公司審查）

狀態：🟡。接手被中斷的前一位 worker（wip commit `f1714f4`，未審查），補完並依 PM 契約修正後才 commit。

## Enter_SA 分析結論（前一位已完成，本次核對）

`docs/research/2026-09-18-d1-room-formats/enter-sa.md`（前一位已寫好，本次只核對）：`0x00220232`
（`0x107e4080`）成功分支後段 6 個 setter **不是**逐欄寫 `ROOM_INFO`，而是清空快取（`Lobby_Data_Clear
0x1070192e`／`Room_Data_Clear 0x10709bbf`／`Community_Chat_Clear 0x107080d0`）→ 用本地已快取的房名開房
（`Room_Open 0x10703d8c`）→ 設目前房間（`Location_Room_Set 0x1070117c`）→ 切場景 5
（`Scene_Change 0x1070148d`）。組語核對：`Room_Open`／`Location_Room_Set` 傳入的 index 是編譯器重用
成功判斷用的同一個恆零暫存值，不是封包欄位。**結論：Enter_SA body 只需要 6-byte 0/0（成功）或非零
（失敗），不帶房間資料**；房間內容全部靠後續 SN。實作的 `sendEnterSa()` 送 6-byte 0/0，跟分析一致。
⬜ 未查：Event_Call 數字→錯誤字串對照表、`FUN_107e3840()` 上限 11 那段字串的來源（連線物件欄位，
與 Enter_SA 實作無關）。

## 改動摘要

在前一位 wip 基礎上，依 PM 契約修正：

1. **`LOBBY_ROOM_LIST_MODE` 獨立開關**（`rooms.js`，預設 `disabled`）：前一位把 `Room_List_SN`
   的所有送出都掛在 `ROOM_JOIN_MODE` 下；PM 契約要求分開，因為大廳清單本身會改變單人可見行為。
   `gate.game.dispatch.js`（CQ_CREATE 廣播、Enter_CQ 成員數變動廣播）、`lobby.dispatch.js`
   （開大廳送完整清單）、`room.dispatch.js`（Leave_CQ 回大廳後送完整清單）四處改成讀
   `rooms.isLobbyRoomListEnabled()`。加入房間本身（Enter_CQ 處理、成員登錄、`User_Default_SN`
   廣播）仍然只看 `ROOM_JOIN_MODE`。
2. **斷線即離開**（design §4 更正）：新增 `dispatch/room/room-leave.js` 的 `leaveRoomAndNotify()`，
   把前一位在 Leave_CQ case 裡內聯的「移除成員＋`Leave_SN`＋房主交接＋大廳通知」邏輯抽出來，
   `Leave_CQ`（0x00220234）與 `server.js` 的 socket `close` hook 共用同一個函式。`server.js` 的
   close hook原本只 `setMemberClient(null)`（寬限設計殘留註解），改為 `ROOM_JOIN_MODE` enabled 時
   直接呼叫 `leaveRoomAndNotify()`；disabled 時維持原行為（純粹不動的分支，回歸不受影響）。
3. `buildMemberUserCtx()` 從 `gate.game.dispatch.js` 搬到 `room-user.sender.js`（`room-leave.js`
   房主交接時也要用，避免兩處各自定義一份走鐘）。
4. 修正 `room-list.sender.js` 的一句過度確定的註解：header +0x00 原寫「"more frames follow" flag」，
   notes.md 其實是「讀了但沒用」，已改成如實描述，程式行為不變（client 端不管這個 byte）。

Enter_SA 的 6-byte 0/0 body、Room_List_SN 分包（entry ≤25 字元房名、單包扣掉 0x10 header／0x20
安全邊界後上限約 974 bytes、count 是 u8）沿用前一位既有實作，未改動。

## 測試

全部在 `~/mro-wt/d1s4/Metal Rage Online Server` 下跑，rebase 到 `reverse-work`（含 K2 console 指令、
T1 fallback、房間隊伍聊天開關）之後全部重跑：

- `node test/replay-golden.js`：`ALL SAMPLES PASS`（含 `pve-full-match` 8383 packets）。
- `node test/rooms.js`：`ALL CHECKS PASS`（5 案例）。
- `node test/room-chat.js`：`ALL CHECKS PASS`（3 案例）。
- `node test/room-join.js`：`ALL CHECKS PASS`——A 建房→B 大廳收到 `Room_List_SN`；開大廳單獨收到完整
  清單；B `Enter_CQ` 加入→雙方互收 `User_Default_SN`；B 斷線（直接呼叫 `leaveRoomAndNotify`，模擬
  socket close，非 `Leave_CQ`）→A 收到 `Leave_SN`；房主 A 斷線→B 收到 `User_Master_SN` 成為新房主；
  25 間房（房名各 25 字元頂格）→`Room_List_SN` 分成 2 個 frame，皆 ≤0x400 wire bytes。
- `node test/login-token.js`：`ALL CHECKS PASS`。
- `node test/whitelist.js`：`ALL CHECKS PASS`。
- `node test/exception-guard.js`：`ALL CHECKS PASS -- process is still alive`。
- `node test/console-commands.js`（rebase 後才存在的樣本）：`ALL CHECKS PASS`。

兩個開關（`ROOM_JOIN_MODE`、`LOBBY_ROOM_LIST_MODE`）皆維持預設 `disabled`；不 `--record` 的情況下
`replay-golden.js` 全綠，符合契約第 3 點。

**測試計畫補充（2026-09-19 PM 契約修正 2）：** `rooms.js` 刻意放在 `dispatch/` 外（見檔頭註解），
`/reload` 只重新 `require` `dispatch/*.js`（`dispatch.js`/`game.js`），不會重新載入 `rooms.js`，而且
`/reload` 本來就要保留活著的房間表不被清掉（這正是 `rooms.js` 放在 `dispatch/` 外的原因）。因此
實測時要切換 `ROOM_JOIN_MODE`／`LOBBY_ROOM_LIST_MODE`（`rooms.js` 裡的 `roomJoinMode`／
`lobbyRoomListMode`），**必須改原始碼後完整重啟伺服器**（停掉 tmux `server` 再重開），`/reload`
不會套用新值；這點跟 `dispatch/` 底下其他開關（`/reload` 就能套用）不同，實測排程要分開算。

## 不確定／待審處

- `Room_List_SN` 的 `RoomNumber`（UpdateType=1 才送）與 `RoomNameIndex`（bit8）沿用前一位的猜測，
  都直接回填 `RoomIndex` 的值——這兩個欄位在 notes.md 沒有追到客戶端下游用途，只是「不無中生有一個
  數字」的權宜做法，⬜ 未經雙機實測驗證。
- bit1 第二個 byte（matching/playing 相關）、bit4（2 bytes）意義仍 ⬜，比照契約沒有設 bit4。
- `Enter_SA` 失敗時的 `status/result` 數字（目前送 1/1）不保證對應到哪一句彈窗文字，只保證非空、
  不會卡住，見 enter-sa.md「沒有解決」一節。
- `leaveRoomAndNotify()` 目前假設呼叫時房主交接一定有成員可以接手（`remainingMembers[0]`）；沒有
  實機驗證「兩台真客戶端」的完整流程（大廳看到房間、雙擊加入、其中一台斷線／叉掉程式）——這步驟
  是 D1 設計稿裡明寫「實機測試」的項目，本次只有單元測試層級的驗證，未請操作者操作。

## M1 實測 1（2026-09-19 10:11–10:15，雙機：本機 Lucas＋筆電 test）

- build：`test-server@f2a31c9 dirty=true nonDefault=[GAME_INFO_TIME_LIMIT_MODE, PVE_ROUND_ADVANCE_MODE, mapAllSingleEntryMode, lobbyRoomListMode] whitelist=on(3 users)`（`session-20260919-100817.jsonl`）。
- ✅ [LOG][OBS] 筆電先在大廳，Lucas 建房（123 行）→ 伺服器馬上對筆電 conn2 送 `Room_List_SN 0x00220204`（124 行）→ 筆電 marker「發現房間列表出現 lucas」（160 行）。第一次跨機器看到別人的房間（**未經跨公司審查**）。
- ❌ 房間資訊不完整：[OBS]「type battle、Map 空、Mode 空、Plyr 8/1」→ 欄位格式待查（D1-4b）。
- ❌ Lucas 離開後，筆電清單仍留著房間（229 行 marker）：[CODE] `room-leave.js:37` 在 roomJoinMode 關閉時直接 return，刪房通知沒送。→ D1-4b 修正 1。
- ⬜ [LOG] Lucas 建房後約 80 秒（ms 242256 → 322050），期間只有心跳，客戶端自己送出 Leave_CQ（188 行）；[OBS] 「幾秒沒動作就會被踢出房間」。原因待查：可能是客戶端的閒置機制，也可能跟雙機有關。
  - **已釐清（2026-09-19）**：[OBS] 操作者確認是被踢出，提示是「一段時間沒有操作」。[SRC] `ZGUIController.uc:945-948`：每次有輸入就呼叫 `ZNetwork_DJ.Room_Time_Reset()`，只有在 `m_IgnoreWaitingTime`（快速配對等狀態，`ZPage_Room.uc:1334/1538/1553`）時才持續重設 → 房間內閒置超時由客戶端自己離開，是**原版的防掛機設計**，不是伺服器造成的。超時秒數（這次約 80 秒）在 native 端，🟡 未查。測試時兩台都要記得偶爾動一下。
- 順序 B（先建房、筆電後登入看全量清單）這一輪沒有測到。

## M1 實測 1 後修正（🟡 中階，未經跨公司審查，D1-4b）

worktree：`~/mro-wt/d1s4b`。build 紀錄確認實測 1 的開關組合是 `lobbyRoomListMode` 開、`roomJoinMode` 關
（見上面 build 行），這正是修正 1 要處理的組合。

**修正 1（bug）：`leaveRoomAndNotify()` 刪房通知被 roomJoinMode 誤擋。**
[CODE] `dispatch/room/room-leave.js` 原本第一行 `if (!rooms.isRoomJoinEnabled()) return;`，roomJoinMode
關閉時整個函式（含 `rooms.removeMember()`）都不執行，房間永遠留在登錄表、大廳清單也收不到刪除通知。
PM 追加裁決把邊界改成「會改變大廳清單內容的事件（建房、刪房、人數變動）一律只歸 lobbyRoomListMode
管，不受 roomJoinMode 影響」，理由是 `CQ_CREATE` 的廣播（`gate.game.dispatch.js` 約 800 行）本來就只看
`isLobbyRoomListEnabled()`，`leaveRoomAndNotify()` 應該對稱。改法：`rooms.removeMember()` 一律執行；
`Leave_SN`／房主交接仍只在 `remainingMembers.length>0 && isRoomJoinEnabled()` 時送（這部分本來就假設對方
是透過 roomJoinMode 的 Enter_CQ 加入的，沒有真正的「多人卻 roomJoinMode 關」情境）；大廳廣播
（UpdateType=2 或 3）一律只看 `isLobbyRoomListEnabled()`，跟 `remainingMembers.length` 決定型別無關。
`server.js` 的 socket close hook原本也用 `isRoomJoinEnabled()` 決定要不要呼叫
`leaveRoomAndNotify()`，否則退回舊的 `setMemberClient(null)` 分支——這個分支本來就是舊版「寬限期」設計
殘留（檔頭註解已經寫「superseded」），改成無條件呼叫 `leaveRoomAndNotify()`（它本身已保證對未追蹤成員
是 no-op）。單元測試：`test/room-join.js` 場景 6（lobbyRoomListMode 開、roomJoinMode 關）——A 建房→B 在
大廳收到一筆 `Room_List_SN`（UpdateType=1）→A（房主，唯一成員）`leaveRoomAndNotify`→B 收到一筆
`Room_List_SN`（UpdateType=3，RoomIndex 對應剛建的房）、`rooms.getRoom()` 回傳 undefined。

**修正 2（分析＋實作）：Room_List_SN 三個欄位的錯誤 offset／未轉換值。**
用 `tools/disasm.py at 0x107e4640 <n>` 讀 Room_List_SN handler，並直接從 `ZNetwork.dll` 檔案位元組解讀
兩個 jump table（`0x107e4e40` RoomType 用、`0x107e4e58` bit1 matching/playing 用，本次沒有展開 bit1）。
逐一對照 `ROOM_SIMPLE_INFO`（`ZNetwork_DJ.uc:533-556`）宣告順序反推的 struct offset（`ebp+4`=RoomNumber、
`ebp+8`=RoomType、`ebp+0x18`=MapIndex、`ebp+0x1c`=MapName（FString）、`ebp+0x28`=MapMode（FString）、
`ebp+0x34`=MapLevel、`ebp+0x38`=MapImage、`ebp+0x3c`=MaxUser、`ebp+0x40`=CurrentUser、`ebp+0x44`=五個
bool 旗標打包成一個 DWORD 的 bitfield，bit0=IsPassword／bit1=IsMatching／bit2=IsPlaying／bit3=IsTraining／
bit4=IsRotate）；offset 算式緊密打包（每個 int 4 bytes、每個 FString 12 bytes）互相對得上，且與既有已知
的 `ebp+4`/`ebp+8` 用法一致。

1. **bit0 RoomType**：[DLL] `0x107e48ae`-`0x107e48c4` 讀 1 個原始 byte；`0x107e48d0` `dec eax`（eax=raw-1）；
   `0x107e48d1`/`0x107e48d4` `cmp eax,5; ja 0x107e4901`（超出 [1,6] 落到預設分支）；`0x107e48d6`
   `jmp dword ptr [eax*4+0x107e4e40]`——直接從 DLL 檔案位元組讀出 6 個 dword：raw=1→`0x107e48dd`
   （norm=2 campaign）、raw=2→`0x107e4901`（norm=0，等於預設分支）、raw=3→`0x107e48e6`（norm=1
   clan）、raw=4→`0x107e4901`（norm=0）、raw=5→`0x107e48ef`（norm=3 quick）、raw=6→`0x107e48f8`
   （norm=4）、raw=0 或 ≥7→norm=0。UI 端 `ZPage_Lobby.uc:794`／`:972` 用 `RoomType==2` 判斷 PvE／
   campaign 分支，`szRoomType` 陣列與 `ZNetwork_DJ.uc:537` 註解「0:일반,1:클랜전,2:캠페인,3:퀵매칭」對得上，
   norm 值就是 rooms.js 既有 `roomType` 欄位的值域。舊 sender 直接把 norm 值（campaign=2）當 raw 送，
   raw=2 依表會再正規化成 norm=0（一般），這正好解釋 [OBS]「type battle」：實測封包
   `session-20260919-100817.jsonl:124` 的 bit0 位元組確實是 `02`。修正：新增
   `ROOM_TYPE_NORM_TO_RAW={0:0,1:3,2:1,3:5,4:6}`，送對應的 raw 值。
2. **bit2 USER_COUNT 順序對調**：[DLL] `0x107e4a2d`-`0x107e4a76`：第一個讀到的 wire byte 存進
   `[ebp+0x40]`（=CurrentUser），第二個存進 `[ebp+0x3c]`（=MaxUser）。舊 sender 先寫 MaxUser 再寫
   CurrentUser，順序反了。[SRC] `ZPage_Lobby.uc:800` 顯示格式是
   `CurrentUser $"/"$ MaxUser`；實測封包 `08 01`（原意 Max=8/Current=1）依新順序被讀成
   CurrentUser=8、MaxUser=1，畫面顯示「8/1」——跟 [OBS] 完全對上。修正：交換兩個 `writeUInt8` 的順序。
3. **bit5 MAP 6 bytes 重新拆解，MapIndex 在尾端不是開頭**：[DLL] `0x107e4b5b`-`0x107e4c22`：
   buffer[0]/[1]（wire byte 0-1）只用來算一個類似 IsRotate 的布林（`0x107e4b72`-`0x107e4b8e`：兩個
   byte 都 ≤1 才清除 bit0x10，否則設定），buffer[2]/[3]（wire byte 2-3）讀了但完全沒被引用（仍
   ⬜），MapIndex 其實是 buffer 尾端的 word（`0x107e4b94` `movzx eax, word ptr [esp+0x2c]`，對應
   buffer offset 4，即 wire byte 4-5）。取得 MapIndex 後再用它去查 Cache.Bin 的地圖表（表格取得函式
   `0x1091ba60`，entry stride `0xbc`、key 在 entry+0）填出 MapName（entry+0xc）、MapMode
   （entry+0x48）、MapLevel（entry+0x68）、MapImage（entry+4）——這四個欄位**從來不在封包上**，只送
   MapIndex，客戶端自己查表。舊 sender 把 MapIndex 寫在 buffer 開頭（wire byte 0-1），尾端 4 bytes
   填 0；client 讀到的「MapIndex」其實是 buffer[0]/[1]（只拿去算 IsRotate，不是查表 key），真正被拿去
   查表的 wire byte 4-5 是 0，查無此圖，Map／Mode 欄位維持預設空白——跟 [OBS]「Map 空、Mode 空」完全對
   上；實測封包 `32 23 00 00 00 00`（map 9010=0x2332 寫在開頭）證實了這個位置錯誤。修正：改成
   `00 00 | 00 00 | MapIndex(LE u16)`（前 4 bytes 固定 0：不模擬地圖輪替，bytes[2]/[3] 未知先填 0，一併
   標 ⬜，不新增假設）。

三項修正都放在既有 `lobbyRoomListMode` 開關下，沒有新增開關（本來就已經是關閉狀態，改動不影響現行
`replay-golden` 基準）。單元測試：`test/room-join.js` 場景 1 新增位元組層級斷言（RoomType raw=1、
USER_COUNT=CurrentUser 1／MaxUser 8、MAP 尾端 2 bytes=9001、前 4 bytes=0）。

**⬜ 清單（本次沒有處理，留給下一輪）：**
- bit1（matching/playing）兩個 byte 的完整語意——第一個 byte 走另一個 jump table（`0x107e4e58`），
  透過複雜的位元運算把 IsMatching／IsPlaying 兩個旗標一起設定，沒有展開這張表；第二個 byte 讀了但
  沒存（notes.md 既有結論，本次重讀確認沒錯）。
- bit5 buffer[2]/[3]（wire byte 2-3）的意義——讀了但這次追蹤到的路徑完全沒引用。
- IsRotate 的精確語意（buffer[0]/[1] 兩個 byte 各自代表什麼，只知道兩者都 >1 才會設定該旗標）。
- RoomNumber（UpdateType=1）與 RoomNameIndex（bit8）沿用前一位「回填 RoomIndex」的權宜值，這次沒有
  重查。
- **全部沒有雙機實機驗證**——這次只有單元測試層級（`node test/room-join.js`／`node test/rooms.js`／
  `node test/replay-golden.js`），`lobbyRoomListMode` 依然預設 `disabled`，要等下一輪雙機測試才能確認
  真的修好了「type battle／Map 空／Mode 空／Plyr 8/1／房間刪不掉」。

## M1 實測 1 重測（2026-09-19 10:33 後，D1-4b 上線後）

- build：`test-server@f5f5f39 dirty=true nonDefault=[MAP_INFO_REAL_ID_MODE, GAME_INFO_TIME_LIMIT_MODE, PVE_ROUND_ADVANCE_MODE, mapAllSingleEntryMode, lobbyRoomListMode]`（`session-20260919-103330.jsonl`）。
- ✅ [OBS] 筆電清單顯示「Type Cooperation、Map 潛入作戰、Mode 協力模式、plyr 1/4、Waiting」（182 行 marker）；[LOG] 114 行 body `…010000 0108 … 3223 …`。人數送 1/8，顯示 1/4：客戶端依協力模式限制上限，🟡。
- ✅ [LOG][OBS] 房主離開 → 229/230 行送出 UpdateType=3 → 筆電清單即時移除。
- ✅ [OBS] 順序 B（筆電先在大廳、Lucas 後建房）也看得到。
- [OBS] 筆電點房間 → 卡在 Loading：預期結果（roomJoinMode 當時關閉，Enter_CQ 沒有回應）。
- **實測 1 通過**（未經跨公司審查）。

## M1 實測 2：兩台進同一房（2026-09-19 10:47 後）

- build：`test-server@f5f5f39 dirty=true nonDefault=[GAME_INFO_TIME_LIMIT_MODE, PVE_ROUND_ADVANCE_MODE, mapAllSingleEntryMode, roomJoinMode, lobbyRoomListMode]`（`session-20260919-104728.jsonl`）。
- **D1 第 0 步兩人版** ✅ [LOG]：26→32 行，Lucas 的 Gate Leave_SA 發 (1, 0x3b640160)，Login_Again_CQ 帶回同一組；85→91 行，test 發 (4, 0x23b50eb6)，帶回同一組；沒有 WARNING／REFUSING marker。
- ✅ [OBS] 筆電加入 Lucas 的房間；Lucas 看到 test 進紅隊槽（227 行）；房間聊天互通（210 行）；一方離開另一方看得到（227 行）；房主離開後 test 成為房主（270 行）。
- ❌ [OBS] 加入者看到的是 **PvP 畫面（紅藍兩隊）**，不是 PvE 簡報（198 行）；中間地圖和房間設定都是空的（295 行）；test 當房主後可以跳到藍隊（282 行）；Lucas 重新加入後也看到 PvP（330 行）。🟡 推測：送給加入者的房間狀態用的是加入者自己連線上的欄位（campaignRoom_ 等），沒有從 Room 讀。
- 聊天「????」（342 行）：[OBS] 操作者打的是 ASCII「test」，不是中文。[LOG] 客戶端送來的 `0x00220505` body 本身就是 `3f3f3f3f`，伺服器原樣轉發（recv／send 兩行 hex 相同），同一句的「PVP」正常 → **客戶端送出前就換掉了**，疑似客戶端的禁用詞過濾（詞表位置 ⬜）。不是伺服器的問題，低優先。
- 未經跨公司審查。
