# 2026-09-22 08:51 換武器成功，第 2 輪解除阻塞；`HitLoc` 的單位定了

承 `2026-09-22-0840-fkey-and-slot-swap.md`（F 鍵不會選機體、改槽位順序也無效）。
做法：不跟「選機體」纏鬥，直接把 `MTE_a` 裝到**一定會被自動選到**的 `mech_type 1` 上。

`[LOG] tools/pico/logs/reports/round2-probe-mte-*`（`[PASS]`，12 步全過）
`[LOG]` 客戶端 `run-85103.07.log`（08:51:04 開）

## 結果

```
fire_burst: sent 10/10 CLICK left, firing_window_s=34.53
客戶端 log:  10 × Map_PC04.MTE_a   Fire
             10 × Map_PC04.MTE_a   ReLoad
             10 × HitLoc=== >
```

### 一、換武器有效，客戶端不擋不相容組合 ✅

✅ **把 `items` 裡 `mech_type=1` 的主武器改成 `25300101`（`MTE_a`）之後，
自動選到的機體開火就是 `MTE_a`**（未經跨公司審查）。
客戶端**沒有**拒絕這個組合，也沒有崩潰——與 `dispatch/room.dispatch.js:104-114`
的註解一致（伺服器不做機體／武器相容性過濾，過濾在客戶端），但客戶端顯然也不擋已裝備的。

腳本：`database/swap-test-mech1-weapon.js`（冪等、預設 dry-run、`--revert` 可還原）。

### 二、`enter_battle(mech_key=None)` 有效 ✅

✅ **只送 ESC、完全不送機體鍵，靠選機體頁的倒數計時自動出機體，可行**（未經跨公司審查）。
回報：`ESC only (no mech-select key, relying on auto-pick countdown)`，
`ChangeSlot_CN`/`Respawn_CN` 照常收到。
→ 第 2 輪**不要再送 F 鍵**，F 鍵那條路已證實無效（前一篇）。

### 三、`HitLoc===` 在 `MTE_a` 上存在，而且是「一次扣扳機一行」✅

**10 次 `CLICK` → 10 行 `HitLoc` → 10 行 `Fire`。**

✅ **`HitLoc=== >` 是每次扣扳機寫一行，不是每消耗一發彈藥寫一行**（未經跨考司審查）。
雙臂武器一次扣扳機消耗 2 發彈藥，若 `HitLoc` 是按彈藥計就會有 20 行。
→ `docs/next-test.md` 那句「分母一定要用 `HitLoc===` 的行數，不要用 HUD 彈藥數」**是對的**，
本輪補上了它缺的證據。

> 這也修正了前一篇的一個疑問。`2026-09-20` 的 `run-213726.73.log` 有 68 `HitLoc` / 47 `Fire`，
> 我先前推測「68 = 34 次扣扳機 × 2 發彈藥」。**那個推測是錯的**：
> `HitLoc` 按扣扳機計，所以那份資料是 **68 次扣扳機、47 次成功生成**，
> 必然跨越至少兩條命（一條命只有 34 次）。缺口 30.9% 的算法不變。

### 四、單人基準是 0% ✅

**10/10，`HitLoc` 與 `Fire` 完全相等。**

✅ **`MTE_a` 在單人環境沒有任何缺口**（未經跨公司審查）。
這正是操作者 2026-09-22 要求的那個對照組，也是 PM 核准的計畫裡缺的一格。
→ 雙人量測出現的任何 `HitLoc` > `Fire` 差額，**都可以歸給網路送出閘門**，
不必再扣本機基準。

> 前一篇 `MOM_a` 兩段各 29/30 的缺口，在 `MTE_a` 上沒有重現（10/10）。
> 🟡 那個缺口看來是 `MOM_a` 特有或低機率隨機事件，⬜ 成因未查，**與第 2 輪無關**。

### 五、每次 `CLICK` 3.45 秒再次確認

`firing_window_s=34.53` ÷ 10 發 ＝ **3.45 秒**（`interval_s=0`）。
與前一篇兩段量到的 3.45／3.44 完全一致，第三個獨立樣本。

## 第 2 輪的設計調整（不必再用補彈）

`MTE_a` 一條命 68 發彈藥 ÷ 2 ＝ **34 次扣扳機**。計畫原本要求每輪 ≥100 次。

🟡 **建議改成「每輪 30 次扣扳機，A／B 各 4 輪」**（每組 120 次，仍滿足每組 ≥100）：

- 30 < 34，**整輪都在同一個彈匣內**，不必補彈
- 因此**完全避開 `SPMaxUP_BD`＋F3 補彈帶來的彈藥狀態變數**
  （`2026-09-22-0748` 記過：`sup1` ＝ 無限彈藥讓缺口 20.8%→8.9%，
  彈藥狀態是待測機制的一部分）
- 時間：30 × 3.45 s ≈ 104 s 開火 ＋ 約 90 s 前置 ≈ 3.5 分鐘／輪，8 輪約 28 分鐘

⬜ 未確認：一場 PvE 戰鬥能不能容納 30 次扣扳機而不被打死／回合結束打斷。
下一步要先跑一輪 30 發的單人試打確認，再開始正式的 A/B。

---

## 追加更正（11:0x）：第 2 輪 B 組失敗的原因是**被打死**，不是回合轉換

第 2 輪（B 組）在 `fire_burst` 的戰鬥前置檢查失敗（`battle_hud=not_battle green_px=0`）。

**我一度誤判成「回合簡報畫面蓋住 HUD」，那是錯的。** 更正如下（由 worker 指出，高階覆核）：

| 截圖 | `screens.py battle-hud` |
|---|---|
| `[SHOT] shots/r2-stuck-joiner.png`（高階清場時拍的） | **`state=battle green_px=6279`** |
| `[SHOT] shots/round2-projectile-43-fire_burst-precondition.png`（runner 在失敗當下自存的） | **`state=not_battle green_px=0`** |

失敗當下那張的內容是**機體被擊毀後的重生機體選擇頁**
（畫面寫「被擊毀。9/10」、RESPAWN 倒數條、F1–F8 機體格），
不是 `MISSION BRIEFING` 橫幅。

**錯誤是怎麼發生的**：我在失敗後好幾分鐘才去拍客戶端，那時它早已重生進新回合，
我卻拿那張（比失敗時刻晚很多的）截圖去推斷失敗原因。
→ 與 `AGENTS.md` 陷阱表那條「曾把只框到標題列的截圖誤判為白畫面」同一類：**讀錯 frame**。
**教訓：判定失敗原因一律用 runner 在失敗當下自存的那張，不要事後另拍。**

### 連帶更正：「每輪 20 發」的理由換掉

初稿的推算是「30 發 104 秒、扣掉中斷後回合窗口約 72 秒、所以 20 發 69 秒安全」——
那是建立在已被推翻的「回合轉換」模型上，作廢。

20 發現在的理由（🟡，比原本弱，但成立的部分）：
1. **20 < 34**（`MTE_a` 一條命的扣扳機上限），整輪在同一個彈匣內，不必補彈。這條不受影響。
2. 第 1 輪實測在**第 21 次扣扳機附近被打死**，所以 20 發大致是「單一條命內」的上限，
   但**只有一個樣本，邊際很窄**，⬜ 未驗證能穩定跑完。

**接受的後果**：某些輪可能不到 20 發就被打死。那一輪照實記錄 n，**不補足也不丟棄**
（計畫的主要判準是「逐輪缺口不重疊」，不要求各輪 n 相等）。

### ⚠️ 樣本數已偏離 PM 核准的計畫，要回報

`round2-plan.md` 原訂「每輪 ≥100 次扣扳機、每組合計 ≥300」。
受 `MTE_a` 彈藥（34）與被打死（約 20）兩個上限所限，現在是**每輪 20、每組 5 輪 ＝ 100**。
→ **每組樣本從 300 降到 100**，是對已核准設計的實質偏離，收工時要向 PM 回報。
不阻塞，但不要默默改掉。
