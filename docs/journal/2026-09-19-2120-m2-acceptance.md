# M2 驗收場：區網兩人完整打完一場 PvE（2026-09-19 晚）

- 版本：test-server@35a1af4。唯一的非預設開關是 `roomPlayingStateMode`，其他驗證過的功能都已經是預設值。log：`logs/session-20260919-200917.jsonl`。
- 網路設定（照 PM 要求記錄）：主機 Lucas 的 `MetalRage.ini` 新加 `[IpDrv.TcpNetDriver]` MaxClientRate=100000、MaxInternetClientRate=100000；dusk 的 `User.ini` ConfiguredInternet/LanSpeed=100000。
- 成員：房主 Lucas（conn2，主機 Win11），加入者 dusk（conn4，第二台 Win10）。
- [LOG] 流程：
  - dusk Enter_CQ（ms 246677）；
  - 房主開戰 0x00222103（ms 250450）；
  - 高級 10 回合：`Campaign_CN` 共 10 次，前 9 次各對**兩條連線**廣播 `EndRound_SN 0x00222211`（ms 603263 … 2679291）；
  - 最後一次（ms 3152543）對兩人都送了 `User_Score_SN 0x00222221`（Rank 10）和 `EndGame_SN 0x00222213`。
- [OBS] 「我哥那邊有結算」。[LOG] 結算後 dusk 在房間按準備（ms 之後 `0x00222101` → `User_State_SN 0x00220401` 廣播給兩人）→ 兩人都回到房間。
- **結論：M2 的條件「區網兩人一起打完一場 PvE：開始 → 戰鬥 → 結算 → 回房間」達成** ✅ [LOG][OBS]（未經跨公司審查），送 PM 判定。
- 戰鬥中回報的問題（另外處理，不擋 M2）：
  - Lucas：多次 lag／卡。同一時間伺服器幾乎沒有流量，CPU 和 GPU 也正常 → 跟伺服器無關（backlog NET-3）。
  - dusk：怪物閃現；蓄力射擊、2 連發火箭、主武器有時沒射出或沒有特效和傷害；第 9 回合能量柱沒有 HP 條 → 都是 P2P 同步問題（backlog NET-2）。
  - Lucas 看不到 dusk 的隊伍聊天 → `ROOM_TEAM_CHAT_MODE` 預設關閉（遊戲內聊天另外處理）。

## PM 判定與後續（2026-09-19）

- **M2 達成**（PM 獨立統計：BeginRound 和 Campaign 各 10 次，都來自房主；EndRound 對兩條連線各 9 次；Score 和 EndGame 對兩條連線各 1 次；Death_SN 兩條連線各 945 次；結束後 22 秒 User_State_SN 同時送到兩人；沒有 WARNING、REFUSING、EXCEPTION 或 fallback）。「兩邊擊殺和陣亡數字一致」只有封包層級的證據 🟡，dusk 當房主那場要在結算頁對一下。
- 排除一個便宜的可能：[LOG] 房主和 dusk 收到的 `Game_User_SN 0x00222112`，**UserIndex 1 和 3 兩筆都逐位元組相同**（974 hex 字元）→ dusk 偶爾射不出、沒有傷害，不是伺服器送的配裝不一致造成的，確定是網路和複寫層的問題（NET-2）。
- 隊伍聊天：房內隊伍頻道需要一筆 [LOG] 才能開啟 ROOM_TEAM_CHAT_MODE；戰鬥內隊伍聊天（0x00220507）目前只回送給自己，要改成對同隊廣播，排進下一批。
- 能量柱沒有 HP 條：記進 backlog，歸在 P4 PvE 補完（可能跟 Assist_SN／Campaign_Damage 還沒實作有關）。

## dusk 當房主（同一輪，test-server@2e5015f，白名單 dusk hostAddress=192.168.1.128）

- ✅ [LOG][OBS] dusk（conn2）開戰 0x00222103（ms 71566）→ 只有 dusk 收到 Ready_Host_SQ → dusk 回 CA，埠 30907（`bb78`）→ Lucas（conn4）收到 `Ready_Host_SN` "192.168.1.128"，接著收到 Success → dusk 回報 `0x00420132 0100`（Lucas 已連進 P2P）。[OBS] Lucas 在遊戲內說「可以成功進去」、操作者回報「成功」。（未經跨公司審查）
- 等於同時驗了三件事：第二台的 p2p 防火牆規則、readyHostSplit 在角色互換後的行為、白名單裡第二筆房主位址設定。這是 M3 的預演。
- 還沒回報：結算頁雙方的 K/D 是否一致、非房主端（Lucas）有沒有出現「沒射出／閃現」。
- [LOG] 這場在 ms 500388 由房主 dusk 送出 `Campaign_CN 010002`（任務失敗）→ 兩人都收到 EndGame_SN。
- K/D 一致性（封包層）：兩條連線各收到 101 筆 Death_SN，最後一筆的擊殺者和受害者區塊數值完全相同（20/81、61/10）→ 兩邊結算看到的數字應該一致 🟡（操作者忘了在畫面上對照）。
- 隊伍頻道：Lucas 在戰鬥中打的「隊伍頻道」走的是 `0x00220507`，body 開頭 `0000`，跟一般聊天是同一個 opcode；伺服器只回送給本人，所以 dusk 看不到。房內（戰鬥外）的隊伍頻道還沒拿到 log。
- [OBS] dusk 當房主時，**Lucas 當加入者沒有遇到**「沒射出、沒傷害、怪物閃現」。對照：Lucas 當房主時，dusk 當加入者有遇到。→ 問題跟「**Lucas 這台當房主**」有關，不是非房主端的共同問題。Lucas 這台自己也有不定時卡頓，兩件事很可能是同一個原因（房主那台的影格卡頓，連帶讓 listen server 的複寫停頓）。🟡
- [OBS] 補充：dusk 當房主時，Lucas 雖然沒有閃現、沒射出的問題，但**還是有他原本那種不定時卡頓**。→ Lucas 的卡頓不管誰當房主都會發生，是這台客戶端本身的問題；dusk 的閃現和沒射出只在 Lucas 當房主時出現，可能是 Lucas 那台卡頓時主機跟著停頓造成的。兩者應該是同一個根源（Lucas 這台的影格卡頓）。🟡
- [OBS] Lucas 卡頓時**沒有切換視窗**（遊戲一直保持焦點）→ 排除「視窗失去焦點讓 UE2 降低更新率」。下一步照 PM 建議：先用 PresentMon 錄基準，再只關 MetalRage.exe 的 Win11 視窗化遊戲最佳化／VRR（一次一個變數）。
- [OBS] Win11 筆電（192.168.1.103）也有同樣的卡頓；Win10 的 dusk 沒有。操作者懷疑是 shanzenos 的 Win11 修正造成的。
  - 兩台 Win11 和 dusk 的差別：修改過的 `MetalRage.exe`（三個 patch：SizeOfImage 對齊、y0da 入口 NOP、NRV 緩衝區預先配置，依 `client-notes-upstream.md`，全部只作用在啟動／解殼階段）、SEHOP 關閉、作業系統本身。`D3D9Drv.dll` 的 DxDiag patch 三台都有（Win10 只換 exe，見 `reference/lan-join-guide.md`），所以不算差別。
  - 「patch」和「Win11」這兩個變數分不開：原版 exe 在 Win11 開不起來，修改版 exe 在 Win10 一開就當（`journal/2026-09-19-0010-win10-exe-and-second-client.md`）。能單獨測的只有 Win11 這邊的設定；如果關掉視窗化最佳化／VRR 就不卡，就能排除 patch。🟡
  - 客戶端 log 的 `*_BD`、`GDebuggr NotifyGC`、`Collecting garbage` 只出現在換關卡時（Lucas 這份 log 的第 20–30、80–90、134–144、186、342–353 行），戰鬥中沒有，不像是週期性卡頓的來源。dusk 那份 log 只到登入，沒有可對照的關卡切換。
- [DLL] 逐檔稽核 Win11 修正版（`research/2026-09-19-win11-patch-audit/notes.md`）：exe 只改 6 個位元組，都在啟動解殼階段；多出來的 `wow64log.dll`（會攔截每個系統呼叫、遇到 0x25 號就 `SwitchToThread`）在桌機沒有被載入（不在 System32，也沒有 `C:\wow64log_beacon.txt`）。→ 桌機上看不出修正版會造成遊戲中卡頓 🟡；筆電待確認有沒有 beacon 檔。
