# SP-BIND 實測與 Pico 打字 bug（2026-09-20，操作者在場）

## 結果
- ✅ [TEST][SHOT] `SPMaxUP_BD` 在**一般帳號（Grade 0）**的 PvE 戰場可以用：開主控台（F24）→ 打 `SPMaxUP_BD` → Enter，SP 由 0000 變 9999（`shots/sp-12-confirm.png`）。這次是單人房主。
- ❌ [TEST] `setinput_BD F9 SPMaxUP_BD` → 主控台回「無法辨識的命令」。`setinput_BD` 不是玩家控制器上的 exec（在 `Engine/OptionAll.uc`），主控台叫不到。**PM 提的「綁 F9」方案排除**：每次補 SP 都要開一次主控台。
- 還沒測：兩人場加入者按（RPC `ServerSPMaxUP`）是否有效。

## 這次找到並修好的 Pico bug（重要）
- 韌體的 `type_text` 只處理字母、數字、空白，**所有符號都被默默丟掉**：`SPMaxUP_BD` 送出去變成 `SPMaxUPBD`，所以客戶端才回「無法辨識的命令」。序列埠那端收到的長度是對的（`TYPE len=10`），字元是在韌體轉成按鍵時消失的。
- 另外發現：遊戲用 DirectInput 逐格輪詢，**按太快會被漏掉**。實測「按住 150 毫秒的減號」打得出來。
- 修正（已部署到板子）：加上符號對照表（含 shift 的符號），每個字按住 40 毫秒。修好之後 `SPMaxUP_BD` 一次就成功。
- 影響範圍：之前所有用 Pico 打過的字串**只要含符號就可能少字**。目前用到的 `GameCampaign 1`、帳號 `Lucas`、密碼 `x` 都沒有符號，不受影響。日後打密碼或含符號的房名要注意。

## 給 dusk 的說明（要操作者轉達）
- 無限 SP：戰場中開主控台打 `SPMaxUP_BD`，SP 變 9999，不夠再打一次。
- 但主控台熱鍵是 F24，一般鍵盤按不到，需要能送 F24 的硬體（鍵盤／滑鼠的板載巨集，或一顆 Pico）。`User.ini` 綁鍵無效（`journal/2026-09-17-08`）。
