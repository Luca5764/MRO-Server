# E1 遷移與重複物品合併實際執行（2026-09-19 17:08–17:12）

- 前提：Sol batch4b 有條件 GO（`research/2026-09-19-sol-review/batch4b.md`）。操作者同意 DB 結構改動，也同意合併重複物品、不退點數。執行前確認沒人在線（`/conns` 空），並先停掉伺服器。
- **備份：** `~/mro-backups/mro-before-e1-20260919-170807.sql`，`mysqldump --single-transaction --no-tablespaces`，exit 0，「Dump completed」。注意：`mro` 帳號沒有權限 dump stored procedure `create_new_player`；那個 procedure 在 `metalrageserver.sql` 裡本來就有。
- **遷移：** 第一次執行失敗：`SELECT COUNT(*) AS before` 用到 MySQL 8 保留字，mock 測試沒抓到。當時 `item_equips` 表已經建好、內容是空的。修正 alias（ef10ad3）後重跑：items=177 → item_equips 插入 130 筆，跟 `equipped=1` 的 130 筆一致。
- **合併：** dry-run 列出 8 組，跟 Sol 預演的 7 組一致，多出帳號 2（`123`）的簡易機槍 ×5。正式執行後 items 177 → 154，刪掉 23 個重複 serial，item_equips 維持 130，item_equips 裡沒有指向已刪 items 的孤兒列（LEFT JOIN 檢查為 0）。第二次 dry-run 為 0 組 → 冪等。
- 伺服器已經用 test-server@ef10ad3 重啟，打開了 `ITEM_EQUIPS_MODE`、`battleEndBroadcastMode`、`MAP_INFO_REAL_ID_MODE`。🟡 待實測。
