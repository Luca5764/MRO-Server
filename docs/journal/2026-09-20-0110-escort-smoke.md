# 護送（Escort）PvE 冒煙測試：無人，Pico（2026-09-20）

操作者核准的無人清單項目「PvE 各模式冒煙測試」。操作者在睡覺；結論一律 🟡，等他看過再升。

## 怎麼選到的
- 依據：`research/2026-09-20-pve-modes-ui/notes.md`（explorer）。
  - 護送＝地圖 9007–9009 `Map_PC02`「防衛作戰」，GameInfo `ZModeEscortPve`；
  - 跟一般戰役走同一個選圖介面。
- 步驟：
  1. runner 建 PvE 房；
  2. 在房間點「選擇地圖▼」（client 852,449），跳出四張圖：動力奪取戰／援救基地戰／防衛作戰／潛入作戰；
  3. 點「防衛作戰」（client 1078,527）。

## 結果
- [LOG] `session-20260919-211100.jsonl`：換圖請求 `0x00220221` hex `002f233c0005…`（地圖 0x232f＝**9007**），伺服器回 `0x00220222`，接著廣播 `0x00220226`／`0x00220223`／`0x0023013a`。
- F5 → 出現 marker `gameStarted_ false -> true`、`Game_Start_SN sent`。75 秒內**沒有 unhandled 封包**。
- [SHOT] `shots/esc-04-battle60.png`：沙漠地圖 ROUND 1；HUD 多了 `F4 SP 200 核心EMP`，上方有三組綠色條（疑似護送目標的血條 🟡）；RESPAWN 9，玩家機體 HP 178，有被攻擊。
- 用 `GameCampaign 1` 送 5 次 → R-ROUND cleared=1..5，`playRound=5` → EndGame → 約 20 秒後回到房間（room marker 0.00）→ 上一頁回大廳。
- 結論 🟡：**護送模式在目前的伺服器上開得起來，也能用 GameCampaign 走完 5 回合回房。** 護送專屬的 `TriggerTouch_SN 0x0023013e` 沒實作；實際護送過程（目標被打、失敗條件）沒有驗證。
- runner 的 `battle` marker 是在另一張地圖（Map_PC04）拍的，在沙漠地圖上分數 60.47，認不出來。整合進 runner 之前，要換成跟地圖無關的 HUD 區塊（例如右下 SP、武器欄）。

## 其他模式
- 教學：大廳「教學模式」→ 客戶端本機載入 `Map_Ptuto`，不經伺服器建房。今晚**沒跑**：不知道怎麼離開教學場景，無人時可能卡住，而且跟伺服器無關，價值低。
- Boss：在一般房（不是協力），地圖 4011，而且伺服器把 Create_CQ 的地圖限制在 9001–9012，還缺 `Boss_SN`。另開 backlog。
- 雙 Boss：台版客戶端沒有對應的地圖／GameInfo，不做。
- 更正：`research/2026-09-19-pve-smoke/notes.md` 猜「9004–6 援救基地戰像護送」是錯的。依 Cache.Bin，9004–9006 是一般 `ZModePve`，護送是 9007–9009。

## runner 版（01:40 前後，無人）
- 合併 pico-maps（4855245）之後：
  - 戰場判斷改成數右下角 SP 計數器的綠色像素，跟地圖無關（戰場 6343–6505 px，非戰場全部 0）；
  - 新增 `select_map`。
- ✅ [TEST] `U-pve-escort` PASS：`select_map(defense)` 收到 `0x00220221`，解析出 map id 9007 → 開戰 → 5 回合 → EndGame → 結算 → 回房 → 回大廳。這也是第一次用新的戰場判斷實跑。
- ✅ [TEST] `U-shop-tabs-idle` PASS：在大廳閒置 120 秒，每 30 秒滑鼠微動一次，沒被踢；之後商店 6 步都通過。
- 所有結論 🟡（無人），報告在 `tools/pico/logs/reports/`。
- `U-pve-fail`（GameCampaign 2）雖然在 suite 裡，但**不在操作者核准的無人清單上，今晚沒跑**。
