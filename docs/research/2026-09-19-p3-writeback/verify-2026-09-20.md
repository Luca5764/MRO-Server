# 機械核對：p3-step1-writeback.md + account-record-set-trace.md（verifier 中階，2026-09-20）

範圍：`docs/design/p3-step1-writeback.md` 全文引用的 DLL 位址 / 程式碼行號 / opcode / offset，以及
`docs/research/2026-09-19-p3-writeback/account-record-set-trace.md`（含文末更正）引用的位址。
方法：`tools/disasm.py at/xref/str`（在 `Metal Rage Online Server/` 下執行）、grep 程式碼與 journal/research
檔案。不判斷設計選擇是否合理，只判斷引用跟來源是否對得上。

## 表格

| # | 引用 | 來源 | 結果 |
|---|---|---|---|
| 1 | `RecordInfo_SN 0x00210103`（`0x107c0fa0`） | DLL | ✔ `0x107c0fa0` 是有效函式入口（典型 ZDispatch handler 開頭：`mov al,[ecx+4]; sub esp,0x38; test al,al; jne ...`），opcode `0x00210103` 在 `dispatch-map.py` 靜態表列為 `ZDispatchAccount::RecordInfo_SN` |
| 2 | Coupon setter `0x107089cc` | DLL | ✔ `RecordInfo_SN` 函式體內 `0x107c1170-0x107c1187`：讀 Coupon 相關局部值 → push → `call 0x107089cc`（緊跟在 Point setter 之後） |
| 3 | Point setter `0x10706e24` | DLL | ✔ `0x107c1154-0x107c116b`：讀 Point 相關局部值 → push → `call 0x10706e24`；`Reward_Record_User_SN`（`0x107ed440`）內 `0x107ed5b4` 也 `call 0x10706e24`，跟文件「呼叫同一個 setter」的說法一致 |
| 4 | `gamelogin.dispatch.js:210-224` — wins +0x1C/draws +0x20/losses +0x24/kills +0x28/deaths +0x2C | CODE | ✔ 實際區塊是 209-222（含 `if(record){` 到 `client.send`），212-224 起始行有 1 行誤差但涵蓋到同一段；五個欄位與偏移完全對得上原文 |
| 5 | `account.dispatch.js:518-531` — wins +0x14/draws +0x18/losses +0x1C/kills +0x20/deaths +0x24（舊佈局） | CODE | ✔ 518-533 區塊確認，wins/draws/losses/kills/deaths 偏移與原文完全一致 |
| 6 | `Reward_Record_User_SN 0x00220412`（`0x10705e1b → 0x107ed440`） | DLL | ✔ `disasm.py at 0x10705e1b` 首行即 `jmp 0x107ed440` |
| 7 | `metalrageserver.sql:38-61` — records/mech_levels 表 | CODE | ✔ 逐行核對，38 行是 `CREATE TABLE records`，61 行是 `mech_levels` 建表結尾，欄位（level/exp/exp_max/wins/losses/draws/kills/deaths；mech_levels 的 level/exp/kills/deaths/sorties）與原文描述一致 |
| 8 | `EndGame_SN 0x00222213`（`0x1070a182 → 0x107d7ed0`） | DLL | ✔ `disasm.py at 0x1070a182` 首行 `jmp 0x107d7ed0`；opcode 在 `dispatch-map.py` 靜態表列為 `ZDispatchGame::EndGame_SN` |
| 9 | EndGame_SN body 欄位表（+0x12/+0x14/+0x16/+0x17/+0x1a/+0x1c，B 隊 +0x20..+0x2a） | DLL | ✔ 大致相符，逐一在 `0x107d7f09-0x107d800a` 對到 `movzx`/`mov` 讀取，A/B 兩組欄位間距一致（+0xe，非文件暗示的 +0x10，但文件只寫了 B 組範圍 `+0x20..+0x2a` 沒明講位移量，不算錯）。**但表格漏列一個欄位**：A 隊 `+0x18`（u16，讀進 ecx，也被 push 進同一次 `Game_Score_Set` 呼叫）、B 隊對應 `+0x26`，這兩個欄位確實被讀取並傳給呼叫，但表格沒有列出 — 表格不完整，非錯誤 |
| 10 | 呼叫序列 `Game_Score_Set(A)→Game_Score_Set(B)→Game_Score_Update()→Game_End_Battle→Community_Chat_Clear→Event_Call("NETWORK_GAME_END")→Scene_Change(5)` | DLL | ✔ 兩次 `call 0x10706e56`（A/B）→ `call 0x1070544d`（無額外參數，符合「()」）→ 條件分支下 `call 0x10708fda`→`call 0x107080d0`→push UTF-16 字串 `0x1082e924`（`disasm.py str "NETWORK_GAME_END"` 命中 `utf-16le file 0x12e924`，RVA 對得上）→`call 0x107097ff`→push 5 →`call 0x1070148d`。順序與文件完全一致 |
| 11 | `User_Score_SN 0x00222221`（`0x107051b9 → 0x107ece60`） | DLL | ✔ `disasm.py at 0x107051b9` 首行 `jmp 0x107ece60`；body 欄位表本身文件未重新核對（引用既有 rank/notes.md），未在本次範圍內重查 |
| 12 | `Account_Record_Set 0x107056aa`（thunk → `0x10717260`） | DLL | ✔ `disasm.py at 0x107056aa` 首行 `jmp 0x10717260` |
| 13 | trace.md：`0x107c1082–0x107c1106` 有 14 個 push 直接餵給 `call 0x107056aa` | DLL | ✘ **不成立**。實際反組譯：`0x107c1082-0x107c10b1`（8 個 push）與 `0x107c10b8-0x107c10c6`（另 8 個 push）合計 16 個 push 全部餵給 `call 0x10709156`（debug log），呼叫後 `add esp,0x40`（=16 個 dword）整批清掉。**真正緊接 `call 0x107c1114 → 0x107056aa` 前的 14 個 push 在 `0x107c10dd–0x107c1106`**（另一組、從已清空的棧重新載入的值），不是文件引用的位址範圍 |
| 14 | trace.md：`0x107c10b8–0x107c10c6` 是 debug log 呼叫（`call 0x10709156`），之後 `add esp,0x40` 清 16 個 push | DLL | ✔ 位址、呼叫目標、`add esp,0x40` 都對得上（但如 #13 所述，被清掉的 16 個 push 其實包含 `0x107c1082` 起的那 8 個，不是只有 `0x107c10b8` 起的） |
| 15 | trace.md 更正：Coupon「在 `0x107c1170／0x107c1174` 讀 raw eax+0x24/0x28」 | DLL | ✘ **位址不精確**。`0x107c1170`/`0x107c1174` 實際是 `mov edx,[esp+0x34]` / `mov eax,[esp+0x30]`（讀已算好的區域變數，準備 push 給 `call 0x107089cc`），不是對 `eax`（raw 指標）的直接解參考。真正的 `mov ecx,[eax+0x24]` / `mov edx,[eax+0x28]` 在 `0x107c103b` / `0x107c1042`。**但**：`0x107c1170/0x107c1174` 確實是 Coupon setter（`0x107089cc`）呼叫前的最後兩個 push，跟已知 ✅ 的 Coupon=body+0x14 對得上，佐證的「結論」本身站得住，只是「讀」這個動詞跟位址對錯了指令 |
| 16 | trace.md 更正：Point「在 `0x107c1154／0x107c1158` 讀 raw eax+0x58/0x5c」 | DLL | ✘ 同上。`0x107c1154`/`0x107c1158` 是 `mov eax,[esp+0x2c]` / `mov ecx,[esp+0x28]`（局部變數），緊接 `call 0x10706e24`（Point setter，✅ 已知）。真正的 `mov ecx,[eax+0x58]` / `mov edx,[eax+0x5c]` 在 `0x107c1027` / `0x107c102e`。結論（body=raw+0x10）本身有另一條獨立證據支持（見下 #19），但這兩個位址引用是錯的 |
| 17 | trace.md 更正：Kill「在 `0x107c10e7` 讀取，直接存進 `0x10717307`」 | DLL | 大致 ✔（用詞略鬆）。`0x107c10e7` 是 `push eax`（不是 `mov`/讀取指令，是這一批 14 個 push 中的一個，值來自更早載入的 `eax`），位址落在真正的 14-push 區間（`0x107c10dd-0x107c1106`，見 #13）內，方向正確。`0x10717307`：反組譯確認 `mov dword ptr [esi+0x548],eax`，`eax` 來自 `[ebp+0x38]`（`0x107172fe`），與文件「直接存」的描述相符 |
| 18 | trace.md 更正：`esi+0x51c` 不是固定清零，`0x10717388` 依 body+0x04/0x08/0x0c/0x10/0x40/0x44 做 64-bit 相減，正值覆寫 | DLL | 部分 ✔／部分 ⬜。`0x10717388`（`mov dword ptr [esi+0x51c],eax`）與其前的 64-bit 相減／條件跳轉（`sub ebx,ecx; sbb edi,edx; ...; jg/js`）確認存在；但相減用到的 `ebx`/`edi`/`ecx`/`edx` 來源（`[ebp+0x14]`/`[ebp+0x18]`/`[ebp+0xc]`/`[ebp+0x10]`）本次未逐一回推到 body 偏移，「body+0x04/0x08/0x0c/0x10/0x40/0x44」六個具體數字**無法在本次核對範圍內確認**，標 `?` |
| 19 | trace.md 更正：修正後 `esi+0x53c/0x540/0x544/0x548/0x54c` 對應 `[ebp+0x2c..0x3c]`，對齊 Win/Draw/Lose/Kill/Death | DLL | ✔ 獨立核對成立。直接反組譯 `0x10717260`（Account_Record_Set 本體）在 `0x107172fe-0x1071731f`：`esi+0x548=[ebp+0x38]`、`esi+0x53c=[ebp+0x2c]`、`esi+0x544=[ebp+0x34]`、`esi+0x540=[ebp+0x30]`、`esi+0x54c=[ebp+0x3c]`，逐一對上；`esi+0x550`（總場次）= `[ebp+0x2c]+[ebp+0x30]+[ebp+0x34]`，`esi+0x554`（勝率）在 `0x107173da` = `Win*100/total`（`eax=[ebp+0x2c]` 乘 100 除以 total），跟文件描述完全一致，且位址精確 |
| 20 | trace.md 更正：`esi+0x538` 兩次 clamp（`0..450`、`0..100`） | DLL | ✔ `0x1071739e-0x107173c5`：push `0x1c2`(450) / push `0x64`(100)，兩個 clamp helper（`0x107ffa30`/`0x107ffba0`/`0x107ffaf0`）調用皆存在，數值精確對上 |
| 21 | trace.md 更正：Reward_Record_User_SN 呼叫點 `0x107ed598`，欄位整體往前移 8 bytes | DLL | ✔ `0x107ed440` 函式體在 `0x107ed561-0x107ed58a` 有 14 個 push，`0x107ed598` 正是 `call 0x107056aa`，位址精確；緊接 `0x107ed5b4 call 0x10706e24`（Point setter），且**全函式沒有 `call 0x107089cc`（Coupon setter）**，跟文件「不設 Coupon」一致 |
| 22 | `Reward_Levelup_User_SN 0x00222231`（`0x107ed760`） | DLL | ✔ `disasm.py at 0x107ed760` 是有效函式入口（同款 ZDispatch handler 開頭樣式），opcode 在 `client-dispatch-map.md`/`dispatch-map.py` 對應 `ZDispatchRoom::Reward_Levelup_User_SN` |
| 23 | `User_State_SN 0x00220401` | opcode 表 | ✔ `client-dispatch-map.md` 71/608 行列為 `ZDispatchRoom::User_State_SN` |
| 24 | `Campaign_CN 0x00230139` / `BeginRound_CN 0x00230151` | opcode 用法 | ✔ `lobby.dispatch.js` 內 `case 0x00230139`/`case 0x00230151` 存在，處理邏輯與文件描述（房主觸發、回合推進）相符 |
| 25 | 開戰 `0x00222103`（§2.2 point 1） | opcode 用法 | ✔ `gate.game.dispatch.js` 內 `case 0x00222103`，註解 `Game_Start_CN`，是房主 F5 開戰觸發，跟文件描述一致 |
| 26 | `matches.result` 註解「0=未知 1=win 2=fail（Campaign_CN body[2]）」 | CODE | ✔ `lobby.dispatch.js:332` 註解「body[2] = 1 objective achieved / 2 failed」，數值一致 |
| 27 | `GameCampaign 1` 無權限檢查、host + `Game_Play_Check()` 為真才送 | RESEARCH | ✔ `docs/research/2026-09-19-dev-grade-cheats/notes.md` 第 9/10/16 行原文：「host 且 `Game_Play_Check()`（IsPlay）為真時送 Campaign_CN」、「這些 exec 函式完全沒有 grade／IsMeGM_BD／bAdmin 檢查」，跟設計稿引用一致 |
| 28 | `docs/journal/2026-09-19-2120-m2-acceptance.md`：結算後在房間按準備 → `User_State_SN` 廣播 | JOURNAL | ✔ 該檔第 11 行：「結算後 dusk 在房間按準備（ms 之後 `0x00222101` → `User_State_SN 0x00220401` 廣播給兩人）」，跟設計稿描述一致 |
| 29 | `ZPage_PveResult.uc:113-165`、`ZPopup_Experience.uc:77` | 原始碼行號 | `?` 無法直接核對——`.uc` 反編譯檔不在 repo 內（只存在於 `static/`，依規則不掃描），只能核對「文件互相引用是否一致」：`docs/research/2026-09-19-rank/notes.md:3` 確實標 `[SRC] ZPage_PveResult.uc:113-165`；`docs/research/2026-09-19-progression/notes.md:7` 確實標 `ZPopup_Experience.uc:77`。設計稿的引用跟這兩份既有筆記的行號完全一致，但筆記本身是否忠實反映 `.uc` 原文本次未查 |
| 30 | `docs/backlog.md` AUTO 段落「P3 排序 1」 | DOC | ✔ `docs/backlog.md:620` 有「## AUTO：Pico 無人回歸測試（PM 2026-09-19 排序）」章節存在；未逐字核對「P3 排序 1」這個精確排序數字（backlog 內容較長，本次只核對章節存在） |
| 31 | `config/allowed-users.json`／`config/whitelist.js` 存在，gitignored per-operator | CODE | ✔ 兩個檔案都存在於 `Metal Rage Online Server/config/` |
| 32 | `docs/journal/2026-09-18-16-money-persistence.md`、`2026-09-17-21-battle-score-totals.md`、`2026-09-19-0900-r-round-impl.md`、`2026-09-19-1013-rank-fixed-experiment.md` 等引用檔案 | JOURNAL | ✔ 全部存在於 `docs/journal/` |

## 摘要（僅列不成立與無法驗證項目）

1. **trace.md「14 個 push 在 `0x107c1082–0x107c1106`」不成立**：實際那個範圍的 16 個 push 全部餵給一次 debug log 呼叫（`call 0x10709156`），被 `add esp,0x40` 清空；真正緊接 `call 0x107056aa`（Account_Record_Set）前的 14 個 push 在 `0x107c10dd–0x107c1106`。
2. **trace.md 更正段落裡「Coupon 在 `0x107c1170/0x107c1174` 讀 raw eax+0x24/0x28」「Point 在 `0x107c1154/0x107c1158` 讀 raw eax+0x58/0x5c」位址不精確**：這兩組位址其實是 setter 呼叫前的最後兩個 push（讀局部變數，不是對 raw 指標解參考），真正對 `eax+0x24/0x28`／`eax+0x58/0x5c` 的解參考在 `0x107c103b/0x107c1042` 與 `0x107c1027/0x107c102e`。不過這兩處引用的位址仍確實各自緊接 Coupon setter（`0x107089cc`）與 Point setter（`0x10706e24`）呼叫，結論本身（body=raw+0x10）獨立成立（見下）。
3. **`esi+0x51c` 的 64-bit 相減用到「body+0x04/0x08/0x0c/0x10/0x40/0x44」六個具體偏移，本次無法確認**——只確認了相減與條件覆寫邏輯存在，沒有回推來源暫存器到 body 偏移。
4. **`ZPage_PveResult.uc:113-165`、`ZPopup_Experience.uc:77` 的行號無法直接對原始 `.uc` 核對**（檔案只存在於不掃描的 `static/`），只確認了跟既有 research 筆記互相引用一致。
5. **EndGame_SN body 欄位表遺漏一個欄位**（A 隊 +0x18／B 隊 +0x26，u16，確實被讀取並傳給 `Game_Score_Set`）——不是錯誤，是表格不完整。

其餘 27 項（RecordInfo_SN/EndGame_SN/User_Score_SN/Account_Record_Set/Reward_Record_User_SN/Reward_Levelup_User_SN 的 thunk 位址、dispatch code 行號、opcode 名稱、`Win*100/total`／clamp／欄位對齊等）逐一用 `disasm.py`／grep 核對，跟設計稿與 trace.md 文字相符。「§1.3/§1.4 矛盾已解決」這個 2026-09-20 更正的**結論**（body+0x1c/0x20/0x24/0x28/0x2c=Win/Draw/Lose/Kill/Death 對齊）本身透過獨立路徑（直接讀 `0x10717260` callee 內的欄位對應）重新驗證成立，只是原更正引用的幾個佐證位址寫錯。
