# 下一輪要測的（2026-09-17 13:10）

測試 A 和 A4 的結果見 `journal/2026-09-17-05`、`-06`；根因候選見 `journal/2026-09-17-08-account-level-gm-keybinds.md`。

## 測試 C：帳號權限改成 0（一般玩家）

伺服器已經重啟，`lucas` 帳號的權限已從 4 改成 0。**只改了這一個變數。**

1. 完全關掉客戶端再重開（按鍵綁定只在第一次取得 controller 時設定）。
2. 登入 `lucas`，照原本流程進 PvE。
3. 用隊伍聊天打 marker，一次做一件事：左鍵開火、R、1、2／3／4、右鍵、Space、Shift、F。
4. 關掉客戶端，讓 log 寫完。

要看的：
- 開火時有沒有扣彈藥、有沒有新的 client→server 封包。
- 大廳、房間、商店有沒有因為權限變低而出現新的問題（例如開發者功能消失）。

如果有效：`db.js` 新帳號的預設權限改成 0，`state.md` 標 ✅。
如果沒效：把權限改回 4（`node tools/set-account-level.js lucas 4`），再查 R／F 沒反應的原因。
