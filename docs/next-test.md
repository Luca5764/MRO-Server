# 下一輪要測的（2026-09-17 18:10）

背景：`journal/2026-09-17-10-sol-review-input-failure.md` 最後一節（Sol 設計）。測試 K 取消，改做 L。

## 測試 L：F1 + Scroll Lock 雙指紋（不改伺服器）

原理：
- 一般按鍵表（`ApplyControl`）：F1 = 按鍵教學。
- PvE 追加（`ApplyPveController`）：F1 改成 SP 技能，**叫不出**按鍵教學。
- GM 按鍵表（`ApplyGMControl`）：Scroll Lock = 依序隱藏名字／GM 名單／隊伍分數／底部 HUD。一般表沒有綁 Scroll Lock。

步驟（每一步用隊伍聊天打 marker 回報）：
1. 完全重開客戶端，登入。
2. 在 Hangar：Esc/選單 → 選項 → **取消**；按 F1，確認**會**叫出按鍵教學（關掉它）。
3. 開始 PvE，**不要開選項**。先確認左鍵不能用。
4. 按 **F1**：有沒有叫出按鍵教學？
5. 按 **Scroll Lock 4 次**（每次停一下）：畫面有沒有東西依序消失？
6. Esc → 選項 → **取消**（修好），確認左鍵可以用。
7. 再按 **F1**、再按 **Scroll Lock 4 次**，看跟步驟 4、5 有沒有不同。
8. 關掉客戶端。

判讀（Sol）：
- 步驟 5 Scroll Lock 讓 HUD 依序消失 → 開局套用了 **GM 表**（假設 1）。
- Scroll Lock 沒反應，步驟 4 F1 **會**叫出教學、步驟 7 F1 **不會** → 開局沒有跑 PvE 的套用（假設 3）。
- 兩者都沒有差異，但修好後正常 → 🟡 開局有套用，之後被改掉（假設 2）。
